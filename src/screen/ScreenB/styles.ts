import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#131016',
        padding: 24,
        alignItems: 'center'
      },
      texto: {
        color: '#FFF',
        marginTop: 50,
        fontSize:50
      }
});